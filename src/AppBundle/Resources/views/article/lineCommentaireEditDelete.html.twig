<div id="commentaire{{ commentaire.id }}">
    <div class="col-sm-10">
            <p>{{ commentaire.texte|raw }}</p>
            <p>{{ commentaire.dateCreation|date('Y-m-d', "Europe/Paris") }}</p>
    </div>
    <div class="col-sm-2">
        <a href="">edit</a>
        <a class="delete" href="#" data-href="{{ path('deleteCommentaire', {'commentaire' : commentaire.id}) }}">delete</a>
    </div>
</div>